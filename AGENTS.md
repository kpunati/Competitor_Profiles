# AGENTS.md

This repo's agent + Claude-Code guidance lives in [`CLAUDE.md`](CLAUDE.md). Read it first.

If your tool defaults to `AGENTS.md` (OpenAI Codex style), treat `CLAUDE.md` as the canonical instruction file — it covers what this repo is, the two modes of use (querying + drafting AND catalog growth + maintenance), the canonical file map, the mandatory brand-voice files, the slash commands available in `.claude/commands/`, output conventions, known data-quality issues, and what NOT to do.
